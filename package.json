{
  "scripts": {
    "installDeps": "cd backend && npm install && cd ../frontend && npm install",
    "startDevBackend": "cd backend && npm run dev",
    "startDevFrontEnd": "cd frontend && npm start",
    "buildFrontEnd": "cd frontend && npm run build"
  }
}